---
import { tocEntries } from '~/content/cheatsheet';
---
<nav class="toc" aria-label="Sections">
  {tocEntries.map(({ number, id, label }) => (
    <a href={`#${id}`}><span aria-hidden="true">{number}</span>{label}</a>
  ))}
</nav>
